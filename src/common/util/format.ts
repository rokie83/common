export const formatDay = (day: string): string => ('0' + day).slice(-2);

export const formatMonth = (month: string): string => ('0' + month).slice(-2);

export const formatYear = (year: string): string => ('20' + year).slice(-4);
