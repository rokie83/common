import {Component} from '@angular/core';

@Component({selector: 'bx-spinner', template: `<div></div>`, styleUrls: ['spinner.component.scss']})
/**
 * @whatItDoes This is a small component to visualize loading state
 *
 * It provides a simple container to show a spinner handled in the styles
 *
 * @stable
 */
export class BxSpinnerComponent {
}
